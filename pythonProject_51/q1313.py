class Solution:
    def decompressRLElist(self, nums: list[int]) -> list[int]:
        """
        Декомпрессия списка, сжатого методом Run-Length Encoding (RLE).

        Входной список представляет собой пары [частота, значение].
        Функция возвращает декомпрессированный список, где каждое значение
        повторяется указанное количество раз.

        Например:
        [1,2,3,4] → [2,4,4,4] (2 повторяется 1 раз, 4 повторяется 3 раза)
        """
        n = len(nums)
        answer = []
        # Итерируемся по списку с шагом 2 (обрабатываем пары частота-значение)
        for i in range(0, n, 2):
            # Добавляем в ответ значение nums[i+1], повторенное nums[i] раз
            answer += nums[i] * [nums[i + 1]]
        return answer


if __name__ == '__main__':
    nums = [1, 2, 3, 4]
    # Пример использования (закомментирован по запросу)
    print(Solution().decompressRLElist(nums))

    #rep5++